import { Box } from "@mui/material";

export const Navigation = () => {
  return (
    <Box
      sx={{
        p: 4,
        height: "100px",
        display: "flex",
        alignItems: "center",
        gap: 8,
        backgroundColor: "custom.navy",
      }}
    ></Box>
  );
};
