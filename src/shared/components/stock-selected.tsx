import { Box, Typography } from "@mui/material";

export const StockSelected = () => {
  return (
    <Box sx={{ flex: 1, border: 1 }}>
      <Typography>Particular record details</Typography>
    </Box>
  );
};
